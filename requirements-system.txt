# System packages to install via apt-get (not pip)
# These require system libraries and should be installed after system dependencies

# OpenCV - install after system opencv libraries
# opencv-python==4.8.1.78

# CUPS Python bindings - install after libcups2-dev
# pycups==2.0.1

# Install these manually after running setup.sh:
# sudo apt-get install python3-opencv
# pip install opencv-python (if needed for specific features)